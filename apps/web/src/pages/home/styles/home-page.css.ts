import { style } from "@vanilla-extract/css";

export const movieListTitleWrapper = style({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export const mainListTitle = style({
  margin: "14px 0",
});
